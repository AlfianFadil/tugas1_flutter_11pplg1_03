import 'package:flutter/material.dart';

class HomeFrangment extends StatelessWidget {
  const HomeFrangment({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Welcome to Home Fragment"),
    );
  }
}