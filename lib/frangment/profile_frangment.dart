import 'package:flutter/material.dart';

class ProfileFrangment extends StatelessWidget {
  const ProfileFrangment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Welcome to Profile Fragment"),
      ),
    );
  }
}